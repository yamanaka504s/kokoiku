# 引き継ぎ書（ココイク / 個別外出計画）

- リポジトリ：`yamanaka504s/kokoiku`
- 公開URL：https://yamanaka504s.github.io/kokoiku/
- 本番ファイル：`index.html`（単一ファイル構成）

> 変更履歴は `git log` を見てください。ここには**コードを読んでも分からないこと**だけ書いています。

---

## 構成

APIキーはページに持たせず、**中継サーバー（Cloudflare Worker）側でのみ保持**しています。

```
ブラウザ（キーなし） → kokoiku-api.yamanaka504s.workers.dev → Claude API
                                                            └→ OpenAI API
```

**この中継サーバーは7つのアプリで共有しています。公開先が2種類あるので注意。**

| アプリ | 公開先 | 更新方法 |
|---|---|---|
| ココイク / 誕生日計画 / ケアプラン / ケアプランショート / 居宅ケアプラン | GitHub Pages | `git push` |
| **月報 / 週報** | **Google App Engine** | **`gcloud app deploy`**（push では反映されない） |

→ 中継サーバーの操作方法・セットアップ手順は **`worker/README.md`** を参照。

## 定期点検

`worker` フォルダの **`点検.bat`** をダブルクリックすると、全アプリを1分で点検できます。

- 中継サーバーとAPIキーの状態
- **モデルが廃止されていないか**（今回の障害の原因）
- 実際に文章生成が通るか
- 公開中の全アプリにキーが混入していないか

月1回程度の実行で、職員が気づく前に異常を見つけられます。

## 残作業

- [ ] **月報・週報を App Engine にデプロイ** — ローカルは修正済みだが**本番は旧版のまま**。`gcloud app deploy` が必要
- [ ] **旧APIキーの無効化** — ↑のデプロイ後に、Anthropic / OpenAI のコンソールで Revoke する
- [ ] 1日の呼び出し上限（任意。`worker/README.md` 手順5。現在は無制限）

## ハマりどころ（実際にやられた点）

| やってはいけないこと | 理由 |
|---|---|
| `data.content[0].text` で本文を取る | モデルによって先頭が thinking ブロックになり `undefined` になる。`pickText(data)` を使う |
| `catch (e) {}` で握り潰す | 今回「エラーも出ずに何も起きない」状態になった直接の原因 |
| モデルIDを直書きする | 廃止されると全アプリが停止する。`model: 'auto'` にすれば中継サーバーが最新を選ぶ |
| Cloudflareダッシュボードのエディタで編集する | 入口ファイルを取り違えて反映されない。`npx wrangler deploy` を使う |
| 変数名をドキュメントからコピーする | U+00A0（見た目は空白）が混入して動かなくなる。手入力するか CLI で登録する |

## 設計上の判断

- **ブラウザ→中継サーバーに認証は付けていない**（社内利用のため、タダ乗りは許容する判断）。キー自体は取り出せないので、悪用されてもこのアプリの機能の範囲に限定され、Worker を止めれば即遮断できる。あとから認証を足す場合も変更は Worker 側の1箇所で済む。
- **Opus など高額モデルは中継サーバーでブロックしている**（認証がないため）。使いたい場合は `worker/worker.js` の `ALLOWED_CLAUDE_MODEL` を編集する。
